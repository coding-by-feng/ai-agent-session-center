---
name: update-feature-docs
description: "Automatically update or create feature documentation in docs/feature/ after completing feature work, bug fixes, or refactors. Trigger this skill PROACTIVELY after any code changes that add, modify, or remove user-facing features, backend endpoints, UI components, keyboard shortcuts, or architectural patterns. Also trigger when the user says 'update docs', 'update feature docs', 'document this', or after completing a task that touched 3+ files. Each distinct feature gets its own .md file."
---

This project uses the global `/update-feature-docs` skill. See `~/.claude/skills/update-feature-docs/SKILL.md`.

Feature docs live in `docs/feature/` organized by domain (server/, frontend/, 3d/, multimedia/, electron/). See `docs/feature/README.md` for the full index and impact matrix.
